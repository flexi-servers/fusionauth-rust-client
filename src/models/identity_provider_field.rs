/*
 * FusionAuth API
 *
 * This is a FusionAuth server. Find out more at [https://fusionauth.io](https://fusionauth.io). You need to [set up an API key](https://fusionauth.io/docs/v1/tech/apis/authentication#managing-api-keys) in the FusionAuth instance you are using to test out the API calls.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use super::AppleIdentityProvider;
use super::EpicGamesIdentityProvider;
use super::ExternalJwtIdentityProvider;
use super::FacebookIdentityProvider;
use super::GoogleIdentityProvider;
use super::HyprIdentityProvider;
use super::LinkedInIdentityProvider;
use super::NintendoIdentityProvider;
use super::OpenIdConnectIdentityProvider;
use super::Samlv2IdPInitiatedIdentityProvider;
use super::Samlv2IdentityProvider;
use super::SonyPsnIdentityProvider;
use super::SteamIdentityProvider;
use super::TwitchIdentityProvider;
use super::TwitterIdentityProvider;
use super::XboxIdentityProvider;



#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum IdentityProviderField {
    AppleIdentityProvider(Box<AppleIdentityProvider>),
    EpicGamesIdentityProvider(Box<EpicGamesIdentityProvider>),
    ExternalJwtIdentityProvider(Box<ExternalJwtIdentityProvider>),
    FacebookIdentityProvider(Box<FacebookIdentityProvider>),
    GoogleIdentityProvider(Box<GoogleIdentityProvider>),
    HyprIdentityProvider(Box<HyprIdentityProvider>),
    LinkedInIdentityProvider(Box<LinkedInIdentityProvider>),
    NintendoIdentityProvider(Box<NintendoIdentityProvider>),
    OpenIdConnectIdentityProvider(Box<OpenIdConnectIdentityProvider>),
    Samlv2IdPInitiatedIdentityProvider(Box<Samlv2IdPInitiatedIdentityProvider>),
    Samlv2IdentityProvider(Box<Samlv2IdentityProvider>),
    SonyPsnIdentityProvider(Box<SonyPsnIdentityProvider>),
    SteamIdentityProvider(Box<SteamIdentityProvider>),
    TwitchIdentityProvider(Box<TwitchIdentityProvider>),
    TwitterIdentityProvider(Box<TwitterIdentityProvider>),
    XboxIdentityProvider(Box<XboxIdentityProvider>),
}

impl Default for IdentityProviderField {
    fn default() -> Self {
        Self::AppleIdentityProvider(Box::default())
    }
}


